# PostgreSQL local — ajuste usuário, senha e nome do banco
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vai_comprar?schema=public"

# JWT para autenticação Bearer
JWT_SECRET="troque-para-um-segredo-forte"

# Porta HTTP da API (opcional)
PORT=3000
